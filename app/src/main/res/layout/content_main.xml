<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="media.sigma.loginregistration.MainActivity"
    tools:showIn="@layout/activity_main">

   <TextView
       android:id="@+id/text_data"
       android:layout_width="match_parent"
       android:layout_height="300dp"
       android:textStyle="bold"
       android:text="Login &amp; Registration "
       android:textColor="#2fbeed"
       android:textSize="32dp"
       android:gravity="center"
       android:background="#f7f018"
       />

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/text_data"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/linear_main"
        android:orientation="horizontal"
        >

        <Button
            android:id="@+id/login_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Login"
            android:textColor="#f7f018"
            android:background="#2fbeed"
            android:textStyle="bold"/>

        <Button
            android:id="@+id/regi_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Registartion"
            android:textColor="#f7f018"
            android:background="#2fbeed"
            android:textStyle="bold"
            android:layout_marginLeft="10dp"/>
    </LinearLayout>
</RelativeLayout>
